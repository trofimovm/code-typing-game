# Финальные 10 багов с ОДНОЗНАЧНЫМ решением

## ✅ Баг 1: Забыт await (async/promise)
**Тип:** Асинхронность
```javascript
async function getData() {
  const response = fetch('/api/data');
  const data = response.json();
  return data;
}
```
**Решение:** Добавить `await` перед обоими вызовами
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - добавить await

---

## ✅ Баг 2: Опечатка в имени переменной
**Тип:** Typo
```javascript
const items = [1, 2, 3, 4, 5];
const count = itmes.length;
console.log(count);
```
**Решение:** `itmes` → `items`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - исправить опечатку

---

## ✅ Баг 3: Неправильное условие в while цикле
**Тип:** Логическая ошибка
```javascript
let count = 0;
while (count > 5) {  // неправильный знак
  console.log(count);
  count++;
}
console.log('Done');
```
**Решение:** `>` → `<`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - изменить оператор

---

## ✅ Баг 4: Неправильная индексация массива
**Тип:** Off-by-one error
```javascript
const numbers = [10, 20, 30, 40];
const first = numbers[1];  // берем второй элемент
console.log(first);
```
**Решение:** `[1]` → `[0]`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - исправить индекс

---

## ✅ Баг 5: Неправильная арифметическая операция
**Тип:** Логическая ошибка
```javascript
function sum(a, b) {
  const result = a * b;  // умножаем вместо сложения
  return result;
}
```
**Решение:** `*` → `+`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - изменить оператор

---

## ✅ Баг 6: Неправильный порядок операндов
**Тип:** Логическая ошибка
```javascript
function subtract(a, b) {
  return b - a;  // вычитаем в обратном порядке
}
```
**Решение:** `b - a` → `a - b`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - поменять порядок

---

## 🆕 Баг 7: Неправильный инициализатор for цикла
**Тип:** Off-by-one error
```javascript
for (let i = 1; i < 5; i++) {  // начинаем с 1 вместо 0
  console.log(i);
}
```
**Решение:** `i = 1` → `i = 0`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - изменить инициализатор
**Работает на всех языках:** JS, Python, C++, C#, Java ✓

---

## 🆕 Баг 8: Неправильный логический оператор
**Тип:** Логическая ошибка
```javascript
if (age > 18 || age < 65) {  // ИЛИ вместо И
  return 'valid';
}
```
**Решение:** `||` → `&&`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - изменить оператор
**Работает на всех языках:** 
- JS: `||` → `&&`
- Python: `or` → `and`
- C++, C#, Java: `||` → `&&` ✓

---

## 🆕 Баг 9: Неправильный оператор для проверки четности
**Тип:** Неправильный оператор
```javascript
function isEven(n) {
  return n / 2 === 0;  // деление вместо остатка
}
```
**Решение:** `/` → `%`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - изменить оператор
**Работает на всех языках:** JS, Python, C++, C#, Java ✓

---

## 🆕 Баг 10: Неправильная переменная в цикле
**Тип:** Логическая ошибка
```javascript
const numbers = [10, 20, 30];
for (let i = 0; i < numbers.length; i++) {
  const value = numbers[0];  // всегда берем первый элемент
  console.log(value);
}
```
**Решение:** `numbers[0]` → `numbers[i]`
**Однозначность:** ✓ ЕДИНСТВЕННЫЙ способ - использовать переменную цикла
**Работает на всех языках:** JS, Python, C++, C#, Java ✓

---

# Итого

✅ **10 багов**
✅ **ВСЕ с ОДНОЗНАЧНЫМ решением**
✅ **ВСЕ работают на 5 языках**
✅ **Покрывают разные типы ошибок:**
   - Асинхронность (1)
   - Опечатки (1)
   - Логические ошибки (5)
   - Off-by-one errors (2)
   - Неправильные операторы (3)
